# Disable default grid and header components.
--style=plain

# Always use colors.
--color=always

# Never use a pager utility.
--paging=never

# Only print first 100 lines.
--line-range :100

# Use 4 length spaced tabs.
--tabs=4

--theme="Solarized (dark)"
